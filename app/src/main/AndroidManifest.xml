<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tool="http://schemas.android.com/tools"
    package="com.hitomi.transferimage">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".SysApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tool:ignore="LockedOrientationActivity">
        <activity
            android:name=".activity.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.WebPicSimpleActivity"
            android:label="网络图片预览一"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.WebPicMultiActivity"
            android:label="网络图片预览二"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.LocalImageActivity"
            android:label="本地图片预览"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.RecyclerViewActivity"
            android:label="RecyclerView 测试"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.ResImageActivity"
            android:label="Res 资源图片测试"
            android:screenOrientation="portrait" />
    </application>

</manifest>